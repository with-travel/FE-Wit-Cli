import React from 'react';
import { StyleSheet, View } from 'react-native';

interface ProgressStepBarProps {}

function ProgressStepBar({}: ProgressStepBarProps) {
  return <View></View>;
}

const styles = StyleSheet.create({});

export default ProgressStepBar;
